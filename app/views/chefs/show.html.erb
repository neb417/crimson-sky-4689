<h1>Chef <%= @chef.name %>'s Show Page</h1>

<%= link_to "Ingredients #{@chef.name} uses", chef_ingredients_path(@chef.id) %>

<div id="top_ingredients">
  <h3>Chef <%= @chef.name %>'s Most Used Ingredients</h3>
    <%# <% @top_ingredients.each do |ingredient| %> %>
      <%# <%= ingredient.name %> %>
    <%# <% end %> %>
</div>  